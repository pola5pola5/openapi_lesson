openapi: "3.0.3"

info: 
  title: "Shop Review API"
  version: "1.0.0"

paths:
  {}

components:
  schemas:
    SampleInt:
      type: integer
      minimum: 0
      exclusiveMinimum: false
      maximum: 100
      exclusiveMaximum: true
    SampleString:
      type: string
      nullable: true
      minLength: 10
      maxLength: 20
    SampleBoolian:
      type: boolean